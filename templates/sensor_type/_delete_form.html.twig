<form method="post" action="{{ path('sensor_type_delete', {'id': sensor_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sensor_type.id) }}">
    <button class="btn">Delete</button>
</form>
