{% extends 'base.html.twig' %}

{% block title %}SensorType{% endblock %}

{% block body %}
    <h1>SensorType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sensor_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sensor_type.name }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ sensor_type.unit }}</td>
            </tr>
            <tr>
                <th>InflexId</th>
                <td>{{ sensor_type.inflexId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sensor_type_index') }}">back to list</a>

    <a href="{{ path('sensor_type_edit', {'id': sensor_type.id}) }}">edit</a>

    {{ include('sensor_type/_delete_form.html.twig') }}
{% endblock %}
